Show the current state of the filesystem organiser workspace.

1. **Guardrails**: List all active guardrails from `guardrails/` with a one-line summary of each.

2. **Recent scans**: List the 5 most recent scan reports from `data/`, showing:
   - Directory scanned
   - Date
   - Clutter score
   - Top recommendation

3. **Recent cleanups**: List the 5 most recent cleanup logs from `logs/`, showing:
   - Directory cleaned
   - Date
   - Number of actions taken
   - Key changes

4. **Suggest next steps** based on what's available — e.g. directories scanned but not yet cleaned, or areas that haven't been scanned in a while.
